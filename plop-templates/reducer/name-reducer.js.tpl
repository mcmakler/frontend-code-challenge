import initialState from '../initial-state';
import {{ camelCase name }}Actions from '../../constants/actions';

function {{ camelCase name }}Reducer(state = initialState.{{ camelCase name }}, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default {{ camelCase name }}Reducer;
